@model ICollection<Customer>

@{
    ViewData["Title"] = "Disabled Customers";
}

<h2>Disabled Customers</h2>

@if (@Model.Count > 0)
{
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Id</th>
                <th>Username</th>
                <th>Name</th>
                <th>Role</th>
                <th>Email</th>
                <th>Address</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var customer in @Model)
            {
                <tr>
                    <td>@customer.Id</td>
                    <td>@customer.Username</td>
                    <td>@customer.Name</td>
                    <td>@customer.Role</td>
                    <td>@customer.Email</td>
                    <td>@customer.Address</td>
                    <td>
                        <a href=@Url.Action("RestoreCustomer", new {id = customer.Id}) class="btn btn-sm btn-success">Restore</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>No customers</p>
}