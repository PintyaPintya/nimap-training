@model ICollection<Product>

@{
    ViewData["Title"] = "Disabled Products";
}

<h2>Disabled Products</h2>

@if (@Model.Count > 0)
{
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Price</th>
                <th>Quantity</th>
                <th>Description</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var product in @Model)
            {
                <tr>
                    <td>@product.Id</td>
                    <td>@product.Name</td>
                    <td>@product.Price</td>
                    <td>@product.Quantity</td>
                    <td>@product.Description</td>
                    <td>
                        <a href=@Url.Action("RestoreCustomer", new {id = product.Id}) class="btn btn-sm btn-success">Restore</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>No products</p>
}