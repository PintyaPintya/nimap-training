@model List<Customer>

@{
	ViewData["Title"] = "Customers";
}

<h2>@ViewData["Title"]</h2>

@if (Model.Count != 0)
{
	<table class="table table-bordered">
		<thead>
			<tr>
				<th>Customer</th>
			</tr>
		</thead>
		<tbody>
			@foreach (var customer in Model)
			{
				<tr>
					<td>
						<a href="@Url.Action("Details", new {id = customer.Id})" class="link-primary">@customer.Name</a>
					</td>
				</tr>
			}
		</tbody>
	</table>
}
else
{
	<p>We dont have any customers yet</p>
}


